---
import PageLayout from "@/layouts/Base";
import PostPreview from "@/components/blog/PostPreview";
import SocialList from "@/components/SocialList";
import { getAllPosts, sortMDByDate } from "@/utils";

const MAX_POSTS = 10;
const allPosts = await getAllPosts();
const allPostsByDate = sortMDByDate(allPosts).slice(0, MAX_POSTS);

---

<PageLayout meta={{ title: "Home" }}>
	<section>
		<h1 class="title">hey!</h1>
		<span>ಕನ್ನಡ: ನಮಸ್ಕಾರ</span>
		<p class="mb-2 mt-6">
			my friend's call me <span class="rounded-lg bg-[#29bc89]/30 p-1 text-quote">adi</span> and im based out of <span class="rounded-lg bg-[#29bc89]/30 p-1 text-quote">bangalore</span> (yes, i love masale dose and filter coffee). i used to be a frontend engineer and now im more of a generalist (or maybe a design engineer?). 
		</p>

		<p class="mb-2 mt-6">
			currently working at <strong><i>stark enterprises</i></strong> where I'm building ai infra for gen ai apps. along with that, im a <strike>emergency</strike> dev according to <a class="text-[#29bc89]" href="https://x.com/_adi18_" target="_blank">@_adi18_</a> for his legalai company <span class="rounded-lg bg-[#29bc89]/30 p-1 text-quote">nyayanidhi</span>
		</p>

		<p class="mb-2 mt-6">
			i love meeting new people and going for breakfast walks. if you have any cafe suggestions or like to work from a cafe, im always up for it. pls feel free to hmu on twitter/x
		</p>
		<SocialList />
	</section>
	

	<section aria-label="Blog post list" class="mt-10">
		<h2 class="title text-xl">memoirs</h2>
		<span>ಕನ್ನಡ: ಆತ್ಮಚರಿತ್ರೆಗಳು</span>
		<ul class="space-y-4 sm:space-y- mt-4">
			{
				allPostsByDate.map((p) => (
					<li class="flex flex-col gap-x-2 sm:flex-row">
						<PostPreview post={p} />
					</li>
				))
			}
		</ul>
	</section>
</PageLayout>
